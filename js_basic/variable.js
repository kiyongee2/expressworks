var msg = "Hello";
msg = "World";
console.log(msg);

var nan = 'a' / 10;
console.log(nan);

//null & undefined
let name;  //값이 할당되지 않음(의도적이지 않게 값을 넣지 않았음)
console.log(name);

let address = '';  //의도적으로 공백 문자를 넣음
console.log(address)

let nickname = null   //값이 비어있음(의도적으로 값을 비워둠)
console.log(nickname);
